# This theme extends Material for MkDocs
extends: material

# Global theme configuration. Can be updated in page.meta.nype_config per page.
# Each possible setting explained in sibling __init__.py file.
nype_config: {}

# Global container for local serve status, can be set in mkdocs.yml, but there is a fallback in nype-base.html
is_serve: